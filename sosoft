task.spawn(function()
    pcall(function()
        local Players = game:GetService("Players")
        local StarterGui = game:GetService("StarterGui")
        local CoreGui = game:GetService("CoreGui")
        local GuiService = game:GetService("GuiService")
        local VIM = game:GetService("VirtualInputManager")
        local LocalPlayer = Players.LocalPlayer

        -- ===== CONFIG: เปิด/ปิดฟังก์ชัน (เปลี่ยนเป็น true = เปิด, false = ปิด) =====
        local ENABLE_ADD_FRIEND = false    -- แอดเพื่อนตอนอยู่ Sea 3
        local ENABLE_BLOCK      = true    -- บล็อกผู้เล่นตอนอยู่แมพอื่น
        -- ======================================================================

        -- Sea 3 (World 3) ของ Blox Fruits => เมื่ออยู่แมพนี้จะสลับเป็นโหมดแอดเพื่อน
        local THIRD_SEA_PLACE_IDS = {
            [7449423635] = true,
            [100117331123089] = true,
        }
        local function InThirdSea()
            return THIRD_SEA_PLACE_IDS[game.PlaceId] == true
        end

        local function IsBlocked(userId)
            local ok, list = pcall(function()
                return StarterGui:GetCore("GetBlockedUserIds")
            end)
            if ok and type(list) == "table" then
                return table.find(list, userId) ~= nil
            end
            return false
        end

        -- กดปุ่มด้วยคีย์บอร์ด (โฟกัส + Enter)
        local interact = function(path)
            if typeof(path) ~= "Instance" then return end
            GuiService.SelectedObject = path
            task.wait()
            if GuiService.SelectedObject == path then
                VIM:SendKeyEvent(true, Enum.KeyCode.Return, false, game)
                VIM:SendKeyEvent(false, Enum.KeyCode.Return, false, game)
                task.wait()
            end
            GuiService.SelectedObject = nil
        end

        -- กดปุ่มด้วยเมาส์ (fallback เผื่อคีย์บอร์ดโฟกัสไม่ติด)
        local interact_2 = function(path)
            if typeof(path) ~= "Instance" then return end
            if path.AbsoluteSize.X == 0 then return end            -- ปุ่มหายแล้ว ไม่ต้องคลิก
            local centerX = path.AbsolutePosition.X + path.AbsoluteSize.X / 2
            local centerY = path.AbsolutePosition.Y + path.AbsoluteSize.Y / 2 + 63
            VIM:SendMouseButtonEvent(centerX, centerY, 0, true, path, 2)
            task.wait(0.1)
            VIM:SendMouseButtonEvent(centerX, centerY, 0, false, path, 2)
            task.wait(0.2)
        end

        -- อ่านข้อความในปุ่ม (รวม label ลูก ๆ) -> ตัวพิมพ์เล็ก + ตัดช่องว่างหัวท้าย
        local function buttonText(btn)
            local t = ""
            if btn:IsA("TextLabel") or btn:IsA("TextButton") then
                t = t .. " " .. btn.Text
            end
            for _, d in ipairs(btn:GetDescendants()) do
                if d:IsA("TextLabel") or d:IsA("TextButton") then
                    t = t .. " " .. d.Text
                end
            end
            t = string.lower(t)
            t = t:gsub("%s+", " "):gsub("^%s+", ""):gsub("%s+$", "")  -- ยุบช่องว่าง + trim
            return t
        end

        -- หาปุ่ม "Block" ล้วน ๆ (ไม่เอา "Block and report" / "Cancel")
        local function findConfirmButton(buttonsContainer)
            local kids = buttonsContainer:GetChildren()
            local fallback
            for _, btn in ipairs(kids) do
                local txt = buttonText(btn)
                -- ข้ามปุ่มที่ไม่ต้องการ: ยกเลิก และ รายงาน
                if txt:find("cancel") or txt:find("ยกเลิก")
                    or txt:find("report") or txt:find("รายงาน") then
                    -- ข้าม
                elseif txt == "block" or txt == "บล็อก" then
                    return btn                         -- ตรงเป๊ะ = ปุ่ม Block ล้วน (ดีสุด)
                elseif (txt:find("block") or txt:find("บล็อก")) and not fallback then
                    fallback = btn                     -- เผื่อไม่เจอแบบเป๊ะ
                end
            end
            return fallback
        end

        -- หา modal บล็อก + กดปุ่มยืนยัน (recursive ทนต่อการเปลี่ยน UI)
        local function ProcessBlock()
            CoreGui = game:GetService("CoreGui")
            local modal = CoreGui:FindFirstChild("BlockingModal", true)
                or CoreGui:FindFirstChild("AlertModal", true)
            if not modal then return false end

            local buttons = modal:FindFirstChild("Buttons", true)
            if not buttons then return false end

            local confirm = findConfirmButton(buttons)
            if not confirm then return false end

            interact(confirm)             -- ลองคีย์บอร์ดก่อน
            task.wait(0.3)
            if confirm.Parent then        -- ถ้า modal ยังอยู่ = ยังไม่สำเร็จ -> ลองเมาส์
                interact_2(confirm)
            end
            return true
        end

        -- เช็คว่าเป็นเพื่อนกันอยู่แล้วหรือมีคำขอค้างอยู่หรือยัง
        local function IsFriend(player)
            local ok, res = pcall(function()
                return LocalPlayer:IsFriendsWith(player.UserId)
            end)
            return ok and res == true
        end

        -- เปิด prompt ส่งคำขอเพื่อน (ยิง API ตรงก่อน ถ้าไม่ได้ค่อย fallback เป็น prompt)
        local function ProcessAddFriend(player)
            local ok = pcall(function()
                LocalPlayer:RequestFriendship(player)
            end)
            if not ok then
                pcall(function()
                    StarterGui:SetCore("PromptSendFriendRequest", player)
                end)
            end
        end

        -- หาปุ่ม "Send Request" ล้วน ๆ (ไม่เอา Cancel)
        local function findFriendButton(buttonsContainer)
            local fallback
            for _, btn in ipairs(buttonsContainer:GetChildren()) do
                local txt = buttonText(btn)
                if txt:find("cancel") or txt:find("ยกเลิก") then
                    -- ข้าม
                elseif txt == "send request" or txt == "send friend request" or txt == "ส่งคำขอ" then
                    return btn                         -- ตรงเป๊ะ = ปุ่มส่งคำขอ (ดีสุด)
                elseif (txt:find("send") or txt:find("request") or txt:find("คำขอ")) and not fallback then
                    fallback = btn                     -- เผื่อไม่เจอแบบเป๊ะ
                end
            end
            return fallback
        end

        -- หา modal คำขอเพื่อน + กดปุ่ม "Send Request"
        local function ProcessFriendRequest()
            CoreGui = game:GetService("CoreGui")
            -- ชื่อ modal ต่างกันตามเวอร์ชัน -> เผื่อชื่อ แล้ว fallback เป็น scan ทั้ง CoreGui
            local modal = CoreGui:FindFirstChild("SendFriendRequestModal", true)
                or CoreGui:FindFirstChild("FriendRequestModal", true)
                or CoreGui:FindFirstChild("AlertModal", true)

            local buttons = modal and modal:FindFirstChild("Buttons", true)

            -- ถ้าหาไม่เจอ ให้ scan หา container "Buttons" ที่มีปุ่ม send request
            if not buttons then
                for _, obj in ipairs(CoreGui:GetDescendants()) do
                    if obj.Name == "Buttons" and findFriendButton(obj) then
                        buttons = obj
                        break
                    end
                end
            end
            if not buttons then return false end

            local confirm = findFriendButton(buttons)
            if not confirm then return false end

            interact(confirm)             -- ลองคีย์บอร์ดก่อน
            task.wait(0.3)
            if confirm.Parent then        -- ถ้า modal ยังอยู่ = ยังไม่สำเร็จ -> ลองเมาส์
                interact_2(confirm)
            end
            return true
        end

        local requested = {}       -- จำ UserId ที่ส่งคำขอไปแล้ว กันส่งซ้ำ
        local friendedOnce = false -- แอดเพื่อนแค่รอบเดียวตอนเข้าเกม

        while true do
            task.wait(1)

            -- ปิด SpinnerWindow ถ้าเด้งขึ้น
            pcall(function()
                local pg = LocalPlayer:FindFirstChild("PlayerGui")
                local sw = pg and pg:FindFirstChild("SpinnerWindow")
                if sw and sw.Enabled == true then
                    local above = sw:FindFirstChild("AboveSpinner")
                    if above then
                        local nav = above:FindFirstChild("Navigation")
                        if nav and nav:FindFirstChild("CloseButton") then
                            interact_2(nav.CloseButton)
                        end
                    end
                    if sw:FindFirstChild("CloseButton") then
                        interact_2(sw.CloseButton)
                    end
                end
            end)

            if InThirdSea() then
                -- โหมด Sea 3 (World 3): หยุดบล็อก -> แอดเพื่อนทุกคน "รอบเดียว" แล้วหยุด
                if ENABLE_ADD_FRIEND and not friendedOnce then
                    pcall(function()
                        for _, player in ipairs(Players:GetPlayers()) do
                            if player == LocalPlayer or IsFriend(player) or requested[player.UserId] then continue end

                            local attemp = 0
                            local done = false
                            repeat
                                ProcessAddFriend(player)      -- เปิด prompt "Send Friend Request?"
                                task.wait(1)                  -- รอ modal เด้ง
                                pcall(function() done = ProcessFriendRequest() end)  -- กด Send Request
                                attemp = attemp + 1
                                task.wait(0.5)
                            until attemp >= 3 or done
                            requested[player.UserId] = true   -- ส่งแล้ว ไม่ต้องส่งซ้ำ
                        end
                    end)
                    friendedOnce = true   -- ครบทุกคน 1 รอบแล้ว -> หยุด ไม่ทำอีก
                end
            elseif ENABLE_BLOCK then
                -- วนบล็อกผู้เล่นทุกคน (แมพอื่น ๆ)
                pcall(function()
                    for _, player in ipairs(Players:GetPlayers()) do
                        if player == LocalPlayer or IsBlocked(player.UserId) then continue end

                        local attemp = 0
                        repeat
                            StarterGui:SetCore("PromptBlockPlayer", player)
                            task.wait(1)              -- รอ modal เด้ง
                            pcall(ProcessBlock)
                            attemp = attemp + 1
                            task.wait(1)              -- รอ block list อัปเดต
                        until attemp >= 10 or IsBlocked(player.UserId)
                    end
                end)
            end
        end
    end)
end)


task.spawn(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Genhubs/hee/refs/heads/main/layercustom.lua"))()    
end)

task.spawn(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/P9INC/brsold/refs/heads/main/loadgen"))()    
end)
